console.log("typeof operator ...");

// to check the type of variable we just have to pass variable name with typeof keyword

let var1 = "Sunil";
let var2 = {
    name : "sunil",
    address : "lucknow"
};

// to check what is inside the objects we just have to run console.dir(var2);
// then console.log(var2.name); 

let var3 =  function(){
    return "Hello World";
};

let var4 = true;